fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
sqlalchemy>=2.0.0
openai>=1.40.0
python-dotenv>=1.0.0
httpx>=0.27.0
reportlab>=4.1.0
PyJWT>=2.8.0
pytest>=8.0.0
# Postgres driver for Cloud SQL. Pure-Python = no compilation needed in the
# slim container. SQLAlchemy talks to it via the postgresql+pg8000 dialect.
pg8000>=1.30.0
# Symmetric encryption for at-rest secrets (Fernet/AES-128). The Fernet key
# itself is sourced from CRAIG_SECRETS_KEY env var (mounted by Cloud Run from
# Secret Manager). See secrets_crypto.py.
cryptography>=42.0.0
# Required by FastAPI for UploadFile / Form() endpoints — used by
# widget_api.py for the artwork-upload endpoint (multipart/form-data).
python-multipart>=0.0.9
# Cloud Storage client for production artwork uploads. Falls back to
# local disk in dev when CRAIG_ARTWORK_BUCKET is unset.
google-cloud-storage>=2.14.0
